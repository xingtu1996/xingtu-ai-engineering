---
name: 工程实践-图工程
description: 图工程实践模板——把代码/依赖/契约建成图支撑定位/影响分析/并行编排
metadata:
  type: project
---

# 工程实践-图工程（Graph Engineering / Code Knowledge Graph / Agent Graph / Graph RAG）

> 定义：图工程 = 把代码、依赖、契约、**多 Agent 编排**建成**图**，支撑快速定位、级联影响分析、契约并行编排、Agent 流程编排。
> 市场演进：继 **Loop Engineering**（2026-06 引爆）之后的主战场——多 Agent 编排以图的形式显式化（节点=Agent/任务，边=依赖/数据流/状态转移），赢家不靠单个模型而靠编排结构。
> 示例企业依据：CBM 代码图谱 + 三段式检索（CLAUDE.md 零-C）+ 契约优先（`rules/specs-rules.md` §九）+ 多 Agent 并行编排（`skills/multi-agent-orchestration` + `workflows/_TEMPLATE`）。

## 理念与权威引用

**术语确认**（2026-08 WebSearch 多源核实，非自造）：

| 英文术语 | 中文 | 含义 | 本模板对应 |
|---------|------|------|-----------|
| **Graph Engineering** | 图工程 | 把代码/依赖/契约建成图，驱动快速定位、级联影响分析、契约并行编排的工程实践 | 模板总称 |
| **Code Knowledge Graph** | 代码知识图谱 | 代码符号/依赖/调用链建成的可查询图（Sourcegraph SCIP、GitHub CodeQL、Joern CPG 一类） | CBM 代码图谱（search_graph / trace_path / get_code_snippet） |
| **Graph RAG** | 图增强检索 | 先建图索引、再图上多跳检索增强 LLM 生成的范式（微软 GraphRAG） | 三段式检索 + trace_path depth=3 级联影响 |
| **Agent Graph** | Agent 编排图 | 多 Agent 工作流以图显式化：节点=Agent/任务，边=依赖/数据流/状态转移（LangGraph 一类 + Anthropic orchestrator-workers/parallelization 模式） | `workflows/_TEMPLATE` 动态工作流（agent()/pipeline()/parallel()）+ `multi-agent-orchestration` 形态决策 |
| **Ontology** | 本体论 | 领域概念/实体/关系的显式定义（类/属性/关系/约束），是图的**语义层**——无本体则图只是数据，有本体图才有可推理的语义 | CBM 元数据模型 + 契约 Schema（字段/枚举/约束）+ DSL 硬化 |

**权威引用**：

| 引用 | 出处 | 与本模板的对应 |
|------|------|--------------|
| From Local to Global: A Graph RAG Approach to Query-Focused Summarization（微软 GraphRAG 原始论文） | arXiv:2404.16130（Edge et al., Microsoft, 2024-04；开源 github.com/microsoft/graphrag） | 图构建→社区摘要→图上检索：对应建图（index_status/TOK-006）+ 图上多跳推理（trace_path depth=3） |
| Graph Retrieval-Augmented Generation: A Survey（GraphRAG 权威综述） | ACM TOIS 2025, DOI 10.1145/3777378 | 形式化工作流 G-Indexing → G-Retrieval → G-Generation：对应「建图 → 三段式检索（CLAUDE.md 零-C）→ 生成决策」 |
| Sourcegraph Code Intelligence / SCIP（代码知识图谱工业实现） | sourcegraph.com（SCIP 为 LSIF 后继，scip-java/scip-python 编译器级符号抽取） | CBM search_graph / get_code_snippet 同理念：符号级精确索引支撑语义定位，避开纯文本检索碎片化 |
| Neo4j LLM Knowledge Graph Builder（图工程产业实践） | neo4j.com/blog/developer/llm-knowledge-graph-builder | 从非结构化文档建图 + 社区摘要 + 溯源 provenance：对应契约图 contracts/ 溯源 + validate-contracts.sh 漂移即红 |
| Anthropic《Building Effective Agents》编排模式（Agent Graph 主流范式） | anthropic.com/engineering/building-effective-agents | 8 模式（prompt chaining/router/evaluator-optimizer/orchestrator-workers/parallelization）→ 对应 workflows/_TEMPLATE 的 agent()/pipeline()/parallel() 形态选择 |
| Ontology 本体论（知识表示基础） | 语义网/知识工程（OWL/RDF 体系；Gruber「本体=概念化的显式规范」1993） | 契约 Schema/领域模型的语义层——概念/实体/关系显式定义，图才有可推理语义 |

**一句话结论**：图工程 2024-2026 从"代码/知识图谱 + Graph RAG"演进到"**多 Agent 编排图**"（继 Loop Engineering 后主战场），本体论是其语义地基；本模板的 CBM 图谱 + 依赖 DAG + 契约图 + Workflow 编排即其工程落地，非闭门造车。

## 一、图构建与使用模板

**1. 建图**：`index_status` 查索引，有效直接用，过期（HEAD 变 >100 commits）增量索引（TOK-006）。

**2. 定位**：search_graph 语义搜节点/边，定位 2-3 候选服务；trace_path 追调用链；get_code_snippet 读方法体（TOK-002）。

**3. 影响分析（3 跳）**：trace_path depth=3 确认影响面；高警戒服务（dc-order/dc-promotion/dc-giveaway）加全量 grep 兜底。CBM 盲区（@Resource/@FeignClient/@StreamListener/HTTP 路由）用 `ast-grep-annotations.sh` 提取，grep 落点确认。

**4. 契约**：握手协议定死（Provider 签名 + Consumer 幂等 + 降级）→ contracts/；变更跑 `validate-contracts.sh`（漂移即红）+ 记 contract-change-log.md。

**5. 并行 DAG**：接口依赖=先契约后并行 / 数据依赖=先 schema 后并行 / 完全独立=全并行 / 同文件=串行。无依赖组 Subagent 并行，回写 contracts/。

## 二、骨架表：图类型 → 工具 → 用途

| 图类型 | 工具 | 用途 |
|--------|------|------|
| 代码图谱 | CBM search_graph / trace_path / get_code_snippet | 定位服务、追调用链、找消费者 |
| 依赖图 | CBM trace_path depth=3 + grep 补漏 | 级联影响分析、高警戒服务兜底 |
| 契约图 | contracts/ + validate-contracts.sh + ast-grep | 握手协议、并行 DAG 分组依据 |

## 三、约束/铁律

- **CBM 优先 grep 补漏（TOK-001）**：图定位缩到 2-3 服务，禁全仓扫。
- **三段式检索**：图定位 → ast-grep 精确提取 → grep 落点确认（CLAUDE.md 零-C）。
- **契约变更 → validate**：漂移即红；改契约必同步 contract-change-log.md。
- **级联影响**：改任何服务前 trace_path 3 跳 + Constitution 禁止模式对照。

## 四、关联

- `CLAUDE.md` 零-C（三段式检索）+ 零-B（级联影响铁律）
- `rules/specs-rules.md` §九（契约优先）+ `rules/token-optimization-rules.md` TOK-001/006
- `skills/codebase-memory` + `skills/three-stage-retrieval`
- `templates/并行Spec开发SOP.md`（DAG → 多 Spec 并行）
